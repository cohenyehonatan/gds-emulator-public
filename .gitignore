.DS_Store
node_modules/
dist/
references/*.txt

# validate-*-creds.ts runtime artifacts (live API responses)
travelport-response.json
stays-search-response.json
tvp-diag-*.json
tvp-*.json

# Playwright MCP browser artifacts
.playwright-mcp/

# machine-readable test reports (agent ingestion / CI)
.vitest-report.json
junit.xml
pnr-store.json
pnr-store.queues.json
interface-output/
print-output/
