.DS_Store
node_modules/
dist/
references/*.txt

# validate-travelport-creds.ts runtime artifacts (live API responses)
travelport-response.json
tvp-diag-*.json
tvp-*.json

# machine-readable test reports (agent ingestion / CI)
.vitest-report.json
junit.xml
pnr-store.json
pnr-store.queues.json
interface-output/
